## Source: github

Search GitHub for matching skills and tools:

- Query `awesome-claude-code` and `awesome-mcp-servers` READMEs for entries related to the task.
- Search repo topics: `claude-skill`, `mcp-server`, plus task-specific keywords.
- Rank candidates by stars and last-commit recency; drop archived repos.
- For each kept item, capture the canonical repo URL as `source_url`.
